You have to make it `protected[this]`, after the fix for scala/bug#6566.
```scala
import language.higherKinds
 
trait ELI[N]
trait Companion[+CC[N, E[X] <: ELI[X]]] {
  protected[this] type Coll = CC[_,Nothing]
}
```
