Methods `future` and `promise` in the `scala.concurrent` package object should be deprecated, since their behavior is provided by `Future.apply` and `Promise.apply`, respectively.
I submitted a PR:
https://github.com/scala/scala/pull/3111
