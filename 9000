Is there still any obstacle to fixing this, beyond "macros are experimental so we don't use them in the standard library" (if that's still true — StringContext.f in src/library/scala/StringContext.scala seems an exception)? At least scala/bug#6187 is fixed.
