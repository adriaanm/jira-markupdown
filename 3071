Simplified failure case:

{code}
def m[T <: AnyRef] = null: T
{code}

Output:
{noformat}
error: type mismatch;
 found   : Null(null)
 required: T
       def m[T <: AnyRef]: T = null: T
                               ^
{noformat}
