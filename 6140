Another example:

{code}
    class C extends DelayedInit {
      def delayedInit(body: => Unit) {}
    }
    class Injector {
      def test = {
        val name = ""
        class crash extends C {
          println(name)
        }
      }
    }
{code}
