It seems like TrieMap is exposing a lot of methods which are neither documented nor look like they should be public at all, like `@volatile var root = r`, `def CAS_ROOT(ov: AnyRef, nv: AnyRef)`, `def RDCSS_READ_ROOT(abort: Boolean = false)`.
