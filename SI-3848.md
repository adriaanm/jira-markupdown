The description of {{ArgumentExprs}} on section 6.6 (function applications) includes the following line, which is not present in the full grammar in the appendix:

{code}
   | ‘(’ [Exprs ‘,’] PostfixExpr ‘:’ ‘_’ ‘*’ ’)’
{code}

