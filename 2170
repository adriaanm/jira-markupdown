what about this?
```scala
Stream.const(1).reduceLeft(_ max _)
```
it seems to me that it should run in constant space, but it runs out of memory on both 2.7 and trunk (r18571). if scala/bug#692 is fixable, isn't this fixable too?
