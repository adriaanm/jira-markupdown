See also scala/bug#1424 which I closed as a duplicate of this.  Not 100% sure if by "crashes" iulian means this:
```scala
 found   : Any
 required: AnyRef
case class annot[T] extends StaticAnnotation
           ^
```
If so, then safely duplicates.
