Example without {{synchronized}}:

{code}
  def f(): Int = {
    try {
      try { return 1 }
      finally { println() }
    } finally {
      try { println() } finally { println() }
    }
  }
{code}

