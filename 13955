In next code:
```scala
object T extends Application {
  class A
  val b: A = null
  print(b match {
    case _: A => true
    case _ => false
  })
}
```
We see compiler error: unreachable code.
But code:
```scala
object T extends Application {
  class A
  val b: A = null
  print(b match {
    case _: A => true
  })
}
```
produce match error: null.

