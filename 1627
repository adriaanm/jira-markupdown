That's fine.  I ask because I couldn't figure out what ticket scala/bug#109 was trying to report, and it used asInstanceOf that way:
```scala
    def complete(sym : global.Symbol) = {
      Console.println("completing " + sym)
      typeParser.parseType(sym.asInstanceOf)
    }
```
