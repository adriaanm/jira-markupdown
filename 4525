This is probably caused by r18287, which fixed SI-2147.  The following assumption is broken by the existence of `fallbackStringCanBuildFrom`:
{code}
// we assume there is no other builder factory on streams and therefore know that That = Stream[A]
{code}
