So `dotProcess` is null here apparently: https://github.com/scala/scala/blob/v2.10.6/src/compiler/scala/tools/nsc/doc/html/page/diagram/DotRunner.scala#L44

Except I can't tell how it could become null there, unless someone is calling things in parallel when they shouldn't.
