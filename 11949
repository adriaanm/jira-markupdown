I believe this shows the fix for scala/bug#4171 was not the right one. `existentialBoundsExcludingHidden` hides CA's bound, even though it can be expressed using the type symbol that we'll introduce to "hide" the original `trait A`...
