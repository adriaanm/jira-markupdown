Although serializing anonymous classes (including anonymous functions) is only reliable between binaries compiled from the same exact source by the same compiler, it seems from scala/bug#527 that Scala intends to support it.  In this case, I guess a serialVersionUID should be inserted.
