Here's another test case the eludes my first attempt at a fix:
```scala
class LM {
  class Node[B1]

  // crash
  val g: (CC => Any) = {
    case CC(tttt) =>
      // tttt.##
      new tttt.Node[Any]()
  }
}
```
