Now that += and friends have a special status, it might be good to add an extra precedence rule for consistency with C and Java.
That's actually not so easy to do. What should the precedence rule be?

