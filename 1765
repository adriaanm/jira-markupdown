Fixed in r17659.  Incredibly enough, scala/bug#1764 is not a duplicate of this and will require a separate fix.  Note the difference in the crashers:
```scala
<x></x> match { case <x>{y*}</x> => } // fixed this one
```
vs.
```scala
<x></x> match { case <x>{y*} </x> => } // still crashes
```
Yikes.
