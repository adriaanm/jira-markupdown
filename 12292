It certainly seems related, but note that the stack traces are rather different and the example in this ticket is substantially more complicated. It isn't obvious that a fix for SI-9600 would also fix this ticket.
