See also scala/bug#3248.  I just added that message as a stopgap between "horrible crash generating infinite output" and "actually fixed".  It's going to take me a bit to get a handle on the optimizer.
