Another way to make this compile is:

{code:scala}
-      case c: Compute[A] =>
+      case d: Compute[A] =>
+        val c = d
{code}

which certainly makes it seem like it's a pattern-matcher issue.
