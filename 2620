A small modification is still incorrect.  Make the initial contents:

{code}
class A {
	def x(i: Int) = i+"3"
}
class B extends A
class C extends B {
	def x(s: String) = s+"5"
}
{code}

Only B is recompiled.
