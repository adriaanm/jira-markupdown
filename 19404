{code:scala}
def int2let(i: Int, lower:Boolean):Char = 
  if(lower) ('a'+i).toChar 
  else ('A'+i).toChar
{code}
is truncated to 
{code:scala}
def int2let(i: Int, lower:Boolean):Char = 
  if(lower) ('a'+i).toChar
{code} 
and refuse to compile.
