```scala
def int2let(i: Int, lower:Boolean):Char = 
  if(lower) ('a'+i).toChar 
  else ('A'+i).toChar
```
is truncated to 
```scala
def int2let(i: Int, lower:Boolean):Char = 
  if(lower) ('a'+i).toChar
```
and refuse to compile.
