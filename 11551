Hopefully this turns out to be the same regression as SI-8999. https://github.com/scala/scala/pull/4193 proposes a fix for that bug.

I haven't had a change to test this theory out yet, but will mark this bug as a blocker for 2.11.5 to make sure we do.
