Outgrowth of scala/bug#1492, but that ticket has already been closed twice and the most important case works great.  But as mentioned in its comments it would be useful and more consistent if this also worked:
```scala
for (implicit a <- ...) {
  ...
}
```
