foreach error in Eclise-Scala for code which is fine.
the execution of this code works very well.

object ForEachTest {
  def main(args : Array[String]) : Unit = {
    args.foreach(arg => println(arg))
  }
}
