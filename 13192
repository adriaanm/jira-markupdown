It shouldn't be necessary to treat the parsing phase as a special case in `Global.scala`. If we could figure out how to break the cycle of mutually recursive dependencies, we could eliminate the code that special cases the parsing phase in `Plugins.scala`.
