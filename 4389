see also scala/bug#2236, which resulted in the following epic lines in Implicits.scala
```scala
       val nextOuter = 
         if (owner.isConstructor) {
           if (outer.tree.isInstanceOf[Template]) outer.outer.outer
           else outer.outer.outer
         } else outer
```
