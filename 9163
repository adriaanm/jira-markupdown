Before the fix to scala/bug#6667, the presence or an expected type would put the typer into silent mode which would convince the implicit search to fallback to the implicit companions. (package object foo contributes to the implicit scope of class SeqInt). 
