This doesn't crash, just prints null:
```scala
class Reference[T] {
  var x: T = _
}

object Test {
  def main(x: Array[String]): Unit = {
    val r = new Reference[Nothing]
    println(r.x)
  }
}
```
