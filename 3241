This also happens for nested objects:
{code}
class A {
   def foo: Unit = {
       C
       object C
   }
}
{code}
I suppose that after I fix SI-1591 by treating nested objects as lazy vals, the fix here should be able to handle objects as well.
