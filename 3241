This also happens for nested objects:
```scala
class A {
   def foo: Unit = {
       C
       object C
   }
}
```
I suppose that after I fix scala/bug#1591 by treating nested objects as lazy vals, the fix here should be able to handle objects as well.
