Relates to scala/bug#5991. The compiler allows more than the spec when it comes to T* types, we probably should **only** allow them on method parameters, not on function params.

See also scala/bug#4176.
