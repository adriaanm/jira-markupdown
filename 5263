Another day, another mode of failure.

Minimized:
```scala
trait Bounds[@specialized A] {
  // okay without `>: A`
  def x[B >: A]: Unit = new Bounds[B] {
    lazy val it = ???  // def or val okay
    it
  }
}
```

Seems in the same neck of the woods as the recently remedied scala/bug#5629.
