I could not compile the example as given because `User` was not defined. But once I supplied a dummy definition it compiled OK. I verified that `Enumeration` was loaded without problems. I believe that the fix for scala/bug#3349 also fixed this one.
