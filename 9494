Cut down a little:

{code}
val X, Y = true
(null: Tuple1[Boolean]) match {
  case Tuple1(X) =>
  case Tuple1(Y) => // unreachable
  case _      =>
}

val X, Y = ""
(null: Tuple1[String]) match {
  case Tuple1(X) =>
  case Tuple1(Y) => // no warning
  case _      =>
}

{code}
