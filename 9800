Probably related: specialized class nested member crashes with `EmptyScope.enter`
```scala
class O {

  { class B[@specialized(Int) T]; new B[Int] }

  val m: Object = {
    class C[@specialized(Int) T]
    new C[Int]
  }

  def n: Object = {
    class D[@specialized(Int) T]
    new D[Int]
  }
}
```
