Anonymous functions with by-value parameters can be defined as types:
```scala
val f: (=>int)=>int = null
```

But not as values:
```scala
((a: =>int) => 4)
```
```scala
identifier expected but '=>' found.
  ((a: =>int) => 4)
       ^
```
