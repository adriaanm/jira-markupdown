There are two classes here, each with separate bugs.  The standard Range bug being reported is a duplicate of scala/bug#2535.  NumericRange is apparently doing some weird stuff, but keep in mind that no kind of range can have more than 2^32 values because Seq.apply takes an Int.
