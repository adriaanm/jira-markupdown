Hi extempore,

I suggested maybe returning

{code}
error: type mismatch;
 found   : Long
 required: Int
{code}

because this is what gets returned by 
{code}
var a: Int = 0
a = 1L + a
{code}

But I might be wrong.
