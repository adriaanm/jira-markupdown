The type of a a function literal is always of the form: `FunctionN[A, B]`

In your example `B` is an existential type.

Scala doesn't support dependent function types. There is a feature request for this in scala/bug#4751. I will merge this ticket with that one, by adding your example to the comments of that ticket.
