this no longer crashes since 2.11.6 / scala/bug#8841 / https://github.com/scala/scala/pull/4195.

it still doesn't compile, but we won't fix this. see scala/bug#8342, we plan to dis-ambiguate the f(x = a) syntax so it can only mean "named argument x", not "assignment to x".
