Since the patch doesn't preview in Trac -- the additions are simply these:
{code}
   /** x ->: e ->: f  =  f(e(x))
    */
   def ->:[A](e: A => T1) = (e andThen this)
   
   /** x ->: f   =    f(x)
    */
   def ->:(v1: T1) = apply(v1)
{code}
