Hello,

While doing some work I found the following code that make the 2.7.3rc1 compiler crash :
{code}
package test

trait A 
trait B 

class BImpl extends B {
  this: A =>
}

object Test2 extends Application {
  val b = new BImpl
}
{code}
