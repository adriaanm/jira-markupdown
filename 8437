Duplicate of scala/bug#1945 (which was closed previously closed as won't fix.) Maybe Paul has an idea for something between :load (which really simulates user input, line-by-line) and :paste (which allows for such a block of code.)
