Format code:
{code}
object FunTest {
  def main(args : Array[String]) {
    def testFun(x : Int, y : Int) = x + y
    val fn = testFun _
    fn(1, 2)              // Ok
    (testFun(_, _))(1, 2) // Ok
    (testFun _)(1, 2)     // Error!
  }
}
{code}
