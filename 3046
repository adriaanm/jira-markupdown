Note that by changing compile-http.txt to **only** include Version.scala, compilation is successful. So this problem could be entirely unrelated to higher-kinded inference, as I had originally suggested. It seems equally as weird as scala/bug#2719, which I reported last week.
