Replying to [comment:7 retronym]:
> Here's another example, distilled from Specs:

That looks an awful lot like SI-2897, which is:
{code}
 def f1(t: String) = {
    trait T {
      def xs = Nil map (_ => t)
    }
    ()
  }
{code}
Not exactly the same bug I don't think, just brothers.
