The spec suggests otherwise:

{quote}
A stable identifier is a path which ends in an identifier.
{quote}

{code}
Path     ::= StableId
           | [id ‘.’] this
StableId ::= id
           | Path ‘.’ id
{code}
