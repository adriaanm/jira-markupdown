Try to compile the following snippet:
```scala
class Bug {
  object BoomEnum extends Enumeration {
    type BoomEnum = Value
    val BOOM1, BOOM2 = Value
  }

  val boom = Option(BoomEnum.BOOM1).getOrElse("BOOM")

  boom == BoomEnum.BOOM1
}
```

I have attached a log (scalacOptions: "-feature", "-deprecation", "-verbose", "-Ydebug", "-Ylog:all", "-Xprint:typer". I truncated the log as it never stops.)
