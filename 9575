Reverted due to scala/bug#7486. Reopening.

We might be able to exclude implicits like:
```scala
def a2b(a: A) = <expr>
```

by approximating the type as `A => ?`. and seeing if that is applicable. But we can never force the return type.

